---

# Where to clone the package on the fs
download_location: "/home/vagrant/git/packages"

# Version of AFLPlusplus to install
afl_plusplus_version: "3.12c"

# Build Target to give the makefile, distrib installs everything
afl_plusplus_build_target: "distrib"

# default user to build and install this with
user: vagrant
